FILE(GLOB POST_PROCESSING_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB POST_PROCESSING_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PostProcessing ${POST_PROCESSING_SOURCE_FILES} ${POST_PROCESSING_HEADER_FILES})
TARGET_LINK_LIBRARIES(PostProcessing PRIVATE ModelViewer-Core)
ADD_DEPENDENCIES(PostProcessing ModelViewer-Core)

TARGET_INCLUDE_DIRECTORIES(PostProcessing PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS PostProcessing DESTINATION bin)