FILE(GLOB PARALLAX_MAP_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(ParallaxMap ${PARALLAX_MAP_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(ParallaxMap glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(ParallaxMap glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(ParallaxMap PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS ParallaxMap DESTINATION bin)