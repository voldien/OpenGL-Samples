FILE(GLOB VOLUMETRIC_SCATTERING_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB VOLUMETRIC_SCATTERING_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(VolumetricScattering ${VOLUMETRIC_SCATTERING_SOURCE_FILES} ${VOLUMETRIC_SCATTERING_HEADER_FILES})
TARGET_LINK_LIBRARIES(VolumetricScattering glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(VolumetricScattering glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(VolumetricScattering PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})