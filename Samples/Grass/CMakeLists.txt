FILE(GLOB GRASS_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Grass ${GRASS_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(Grass glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(Grass glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(Grass PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS Grass DESTINATION bin)