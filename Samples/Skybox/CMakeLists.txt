FILE(GLOB SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Skybox  ${SOURCE_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(Skybox ${OPENGL_LIBRARIES} ${GLEW_LIBRARIES} ${SDL2_LINK_LIBRARIES} glCommon gl-sample-common-asset-importer)

# OpenGL dependies headers.
#TARGET_INCLUDE_DIRECTORIES(Skybox PRIVATE ${OPENGL_INCLUDE_DIR} ${GLEW_INCLUDE_DIRS})
ADD_DEPENDENCIES(Skybox glCommon gl-sample-common-asset-importer)
