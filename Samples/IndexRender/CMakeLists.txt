FILE(GLOB SOURCE_INDEX_RENDER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(IndexRender ${SOURCE_INDEX_RENDER_FILES} ${HEADER_FILES} )
TARGET_LINK_LIBRARIES(IndexRender glCommon gl-sample-common-asset-importer fragcore-font)
ADD_DEPENDENCIES(IndexRender glCommon gl-sample-common-asset-importer fragcore-font)

TARGET_INCLUDE_DIRECTORIES(IndexRender PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )
