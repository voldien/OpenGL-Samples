FILE(GLOB POINT_LIGHT_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PointLight ${POINT_LIGHT_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(PointLight glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(PointLight glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(PointLight PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS PointLight DESTINATION bin)