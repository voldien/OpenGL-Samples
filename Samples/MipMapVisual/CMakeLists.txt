FILE(GLOB MIPMAP_VISUAL_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB MIPMAP_VISUAL_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(MipMapVisual ${MIPMAP_VISUAL_SOURCE_FILES} ${MIPMAP_VISUAL_HEADER_FILES})
TARGET_LINK_LIBRARIES(MipMapVisual glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(MipMapVisual glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(MipMapVisual PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS MipMapVisual DESTINATION bin)