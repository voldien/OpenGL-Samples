FILE(GLOB SOURCE_PHYSICAL_BASED_RENDERING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_PHYSICAL_BASED_RENDERING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PhysicalBasedRendering ${SOURCE_PHYSICAL_BASED_RENDERING_FILES} ${HEADER_PHYSICAL_BASED_RENDERING_FILES} )
TARGET_LINK_LIBRARIES(PhysicalBasedRendering glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(PhysicalBasedRendering glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(PhysicalBasedRendering PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )

INSTALL(TARGETS PhysicalBasedRendering DESTINATION bin)