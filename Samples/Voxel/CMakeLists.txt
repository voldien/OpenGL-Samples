FILE(GLOB VOXEL_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Voxel ${VOXEL_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(Voxel glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(Voxel glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(Voxel PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})