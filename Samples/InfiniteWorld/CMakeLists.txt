FILE(GLOB INFINATE_WORLD_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB INFINATE_WORLD_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(InfiniteWorld ${INFINATE_WORLD_SOURCE_FILES} ${INFINATE_WORLD_HEADER_FILES})
TARGET_LINK_LIBRARIES(InfiniteWorld glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(InfiniteWorld glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(InfiniteWorld PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS InfiniteWorld DESTINATION bin)