FILE(GLOB SORT_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB SORT_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Sort ${SORT_SOURCE_FILES} ${SORT_HEADER_FILES})
TARGET_LINK_LIBRARIES(Sort glCommon)
ADD_DEPENDENCIES(Sort glCommon)

TARGET_INCLUDE_DIRECTORIES(Sort PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS Sort DESTINATION bin)