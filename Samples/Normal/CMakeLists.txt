FILE(GLOB NORMAL_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Normal ${NORMAL_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(Normal glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(Normal glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(Normal PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS Normal DESTINATION bin)