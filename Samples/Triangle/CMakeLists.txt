FILE(GLOB SOURCE_TRIANGLE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_TRIANGLE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Triangle ${SOURCE_TRIANGLE_FILES} ${HEADER_TRIANGLE_FILES})
TARGET_LINK_LIBRARIES(Triangle glCommon)

TARGET_INCLUDE_DIRECTORIES(Triangle PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )
ADD_DEPENDENCIES(Triangle glCommon)

INSTALL(TARGETS Triangle DESTINATION bin)