FILE(GLOB SUPPORT_VECTOR_MACHINE_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB SUPPORT_VECTOR_MACHINE_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
INCLUDE(osqp-eigen)

ADD_EXECUTABLE(SVM ${SUPPORT_VECTOR_MACHINE_SOURCE_FILES} ${SUPPORT_VECTOR_MACHINE_HEADER_FILES})
TARGET_LINK_LIBRARIES(SVM glCommon OsqpEigen::OsqpEigen)
ADD_DEPENDENCIES(SVM glCommon OsqpEigen::OsqpEigen)

TARGET_INCLUDE_DIRECTORIES(SVM PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS SVM DESTINATION bin)
INSTALL(TARGETS OsqpEigen osqp DESTINATION lib)