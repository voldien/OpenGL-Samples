FILE(GLOB POINT_LIGHT_SHADOW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PointLightShadow ${POINT_LIGHT_SHADOW_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(PointLightShadow glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(PointLightShadow glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(PointLightShadow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS PointLightShadow DESTINATION bin)