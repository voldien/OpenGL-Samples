FILE(GLOB SOURCE_INDIRECT_DRAW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_INDIRECT_DRAW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(IndirectDraw ${SOURCE_INDIRECT_DRAW_FILES} ${HEADER_INDIRECT_DRAW_FILES})
TARGET_LINK_LIBRARIES(IndirectDraw glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(IndirectDraw glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(IndirectDraw PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS IndirectDraw DESTINATION bin)