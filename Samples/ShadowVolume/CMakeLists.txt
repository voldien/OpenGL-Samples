FILE(GLOB VOLUME_SHADOW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(VolumeShadow ${VOLUME_SHADOW_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(VolumeShadow glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(VolumeShadow glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(VolumeShadow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})