FILE(GLOB RAY_TRACING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(RayTracing ${RAY_TRACING_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(RayTracing glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(RayTracing glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(RayTracing PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS RayTracing DESTINATION bin)