FILE(GLOB SUBSURFACE_SCATTERING_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB SUBSURFACE_SCATTERING_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(SubSurfaceScattering ${SUBSURFACE_SCATTERING_SOURCE_FILES} ${SUBSURFACE_SCATTERING_HEADER_FILES})
TARGET_LINK_LIBRARIES(SubSurfaceScattering glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(SubSurfaceScattering glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(SubSurfaceScattering PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS SubSurfaceScattering DESTINATION bin)