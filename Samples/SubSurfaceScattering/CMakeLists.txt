FILE(GLOB SUBSURFACE_SCATTERING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(SubSurfaceScattering ${SUBSURFACE_SCATTERING_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(SubSurfaceScattering glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(SubSurfaceScattering glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(SubSurfaceScattering PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})