FILE(GLOB MESH_SHADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(MeshShader ${MESH_SHADER_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(MeshShader glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(MeshShader glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(MeshShader PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS MeshShader DESTINATION bin)