FILE(GLOB ZPRE_PASS_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(ZPrePass ${ZPRE_PASS_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(ZPrePass glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(ZPrePass glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(ZPrePass PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS ZPrePass DESTINATION bin)