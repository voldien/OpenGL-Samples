FILE(GLOB PROJECTED_SHADOW_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB PROJECTED_SHADOW_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(ProjectedShadow ${PROJECTED_SHADOW_SOURCE_FILES} ${PROJECTED_SHADOW_HEADER_FILES})
TARGET_LINK_LIBRARIES(ProjectedShadow glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(ProjectedShadow glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(ProjectedShadow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS ProjectedShadow DESTINATION bin)