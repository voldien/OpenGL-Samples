FILE(GLOB POINT_LIGHTS_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(PointLights ${POINT_LIGHTS_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(PointLights glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(PointLights glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(PointLights PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS PointLights DESTINATION bin)