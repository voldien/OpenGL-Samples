#TODO: use existing sample as base.
FILE(GLOB OPTICAL_FLOW_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB OPTICAL_FLOW_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(OpticalFlow ${OPTICAL_FLOW_SOURCE_FILES} ${OPTICAL_FLOW_HEADER_FILES})
TARGET_LINK_LIBRARIES(OpticalFlow glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(OpticalFlow glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(OpticalFlow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS OpticalFlow DESTINATION bin)