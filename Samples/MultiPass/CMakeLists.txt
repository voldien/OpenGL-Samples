FILE(GLOB MULTI_PASS_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB MULTI_PASS_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(MultiPass ${MULTI_PASS_SOURCE_FILES} ${MULTI_PASS_HEADER_FILES})
TARGET_LINK_LIBRARIES(MultiPass glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(MultiPass glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(MultiPass PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS MultiPass DESTINATION bin)