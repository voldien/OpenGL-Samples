FILE(GLOB INFINATE_WORLD_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB INFINATE_WORLD_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(InfinateWorld ${INFINATE_WORLD_SOURCE_FILES} ${INFINATE_WORLD_HEADER_FILES})
TARGET_LINK_LIBRARIES(InfinateWorld glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(InfinateWorld glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(InfinateWorld PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS InfinateWorld DESTINATION bin)