FILE(GLOB SIMPLE_REFLECTION_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(SimpleReflection ${SIMPLE_REFLECTION_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(SimpleReflection glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(SimpleReflection glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(SimpleReflection PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS SimpleReflection DESTINATION bin)