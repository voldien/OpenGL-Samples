FILE(GLOB PANORAMIC_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Panoramic ${PANORAMIC_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(Panoramic PRIVATE ModelViewer-Core)
ADD_DEPENDENCIES(Panoramic ModelViewer-Core)

TARGET_INCLUDE_DIRECTORIES(Panoramic PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS Panoramic DESTINATION bin)