FILE(GLOB SOURCE_SHADOW_MAPPING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_SHADOW_MAPPING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(ShadowMapping ${SOURCE_SHADOW_MAPPING_FILES} ${HEADER_SHADOW_MAPPING_FILES})
TARGET_LINK_LIBRARIES(ShadowMapping glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(ShadowMapping glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(ShadowMapping PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS ShadowMapping DESTINATION bin)