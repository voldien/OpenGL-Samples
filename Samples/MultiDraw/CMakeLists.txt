FILE(GLOB MULTI_DRAW_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(MultiDraw ${MULTI_DRAW_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(MultiDraw glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(MultiDraw glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(MultiDraw PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})