FILE(GLOB SOURCE_OCCLUSION_CULLING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_OCCLUSION_CULLING_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(OcclusionCulling ${SOURCE_OCCLUSION_CULLING_FILES} ${HEADER_OCCLUSION_CULLING_FILES})
TARGET_LINK_LIBRARIES(OcclusionCulling glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(OcclusionCulling glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(OcclusionCulling PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} )

INSTALL(TARGETS OcclusionCulling DESTINATION bin)