FILE(GLOB PLANET_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(Planet ${PLANET_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(Planet glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(Planet glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(Planet PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS Planet DESTINATION bin)