FILE(GLOB PARALLAX_OCCLUSION_MAP_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
FILE(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h)

ADD_EXECUTABLE(ParallaxOcclusionMapping ${PARALLAX_OCCLUSION_MAP_FILES} ${HEADER_FILES})
TARGET_LINK_LIBRARIES(ParallaxOcclusionMapping glCommon gl-sample-common-asset-importer)
ADD_DEPENDENCIES(ParallaxOcclusionMapping glCommon gl-sample-common-asset-importer)

TARGET_INCLUDE_DIRECTORIES(ParallaxOcclusionMapping PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

INSTALL(TARGETS ParallaxOcclusionMapping DESTINATION bin)